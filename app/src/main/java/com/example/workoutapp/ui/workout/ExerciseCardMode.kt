package com.example.workoutapp.ui.workout

enum class ExerciseCardMode {
    SESSION,      // Fill space in active workout, large photo area
    LIST_COMPACT, // Compact view in exercise list, minimal/no photo
    LIST_EXPANDED // Expanded view when tapped, large photo with upload button
}
